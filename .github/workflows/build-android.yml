name: Build Android APK (DEPRECATED - Use EAS)

# DEPRECATED: Use EAS CLI for builds instead
# Manual trigger only - kept for reference/archival purposes
on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Show EAS Build Instructions
        run: |
          echo "❌ This GitHub CI build is DEPRECATED"
          echo ""
          echo "✅ Use EAS CLI instead:"
          echo "  pnpm eas workflow:run build-android-release.yml"
          echo ""
          echo "📍 Or trigger from GitHub Actions:"
          echo "  Actions > EAS Build Android > Run workflow"
          echo ""
          echo "For more info, see .eas/workflows/README.md"

